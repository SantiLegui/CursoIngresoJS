function mostrar() {
	let num;
	let cont;
	num = parseInt(prompt("Ingrese un numero :"));
	while (isNaN(num)) {
		num = parseInt(prompt("Eso no es un numero"));
	}

	for (let i = 1 = 0; i <= numero; i++) {
		if (num % 2 == 0) {
			console.log(i);
			contador++
		}
		console - log("Contador de pares contados" + i);
	}






}//FIN DE LA FUNCIÓN